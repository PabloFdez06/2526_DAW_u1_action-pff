name: Generar Documentación Java (robusto)

on:
  push:
    branches: ["main", "master"]
  pull_request:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-docs:
    runs-on: ubuntu-latest

    steps:
      # Clonar el repositorio
      - name: Clonar repositorio
        uses: actions/checkout@v4
        with:
          persist-credentials: true

      # Instalar Java
      - name: Instalar Java
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      # 3️⃣ Instalar Pandoc + LaTeX completo
      - name: Instalar Pandoc y LaTeX
        run: |
          sudo apt-get update
          sudo apt-get install -y pandoc texlive-latex-recommended texlive-latex-extra texlive-fonts-recommended

      # Generar documentación HTML con Javadoc
      - name: Generar documentación HTML con Javadoc
        run: |
          echo "🔹 Generando Javadoc..."
          mkdir -p docs/html
          if javadoc -d docs/html -sourcepath src/main/java -subpackages org.example; then
            echo "✅ Javadoc generado correctamente."
          else
            echo "❌ Error generando Javadoc."
            exit 1
          fi

      # Generar documentación PDF
      - name: Generar documentación PDF (sin LaTeX)
        run: |
          echo "🔹 Generando PDF desde los HTML de Main y Saludo (sin LaTeX)..."
          mkdir -p docs/pdf
          sudo apt-get install -y wkhtmltopdf
          # Generar PDFs directamente desde HTML
          wkhtmltopdf docs/html/org/example/Main.html docs/pdf/Main.pdf
          wkhtmltopdf docs/html/org/example/Saludo.html docs/pdf/Saludo.pdf
          # Combinar ambos en un solo documento (opcional)
          pdftk docs/pdf/Main.pdf docs/pdf/Saludo.pdf cat output docs/pdf/Documentacion_Completa.pdf || echo "🔸 pdftk no disponible, se omite combinación"
          echo "✅ PDFs generados correctamente."


      # Subir documentación como artefacto
      - name: Subir documentación como artefacto
        uses: actions/upload-artifact@v4
        with:
          name: documentacion
          path: docs/

      # Commit automático de documentación
      - name: Commit automático de documentación
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "update: documentación generada automáticamente"
          branch: ${{ github.ref_name }}
          file_pattern: "docs/**"
